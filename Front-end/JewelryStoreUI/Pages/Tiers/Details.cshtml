@page "{id:guid}"
@model JewelryStoreUI.Pages.Tiers.DetailsModel
@{
}

<h2>Tier Details</h2>

<div>
    <h4>Tier</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">TierId</dt>
        <dd class="col-sm-10">@Model.Tier.TierId</dd>
        <dt class="col-sm-2">TierName</dt>
        <dd class="col-sm-10">@Model.Tier.TierName</dd>
        <dt class="col-sm-2">MinAmountSpent</dt>
        <dd class="col-sm-10">@Model.Tier.MinAmountSpent</dd>
        <dt class="col-sm-2">DiscountPercentage</dt>
        <dd class="col-sm-10">@Model.Tier.DiscountPercentage</dd>
    </dl>
</div>
<div>
    <a asp-page="Edit" asp-route-id="@Model.Tier.TierId">Edit</a> |
    <a asp-page="Index">Back to List</a>
</div>
