@page "{id:guid}"
@model JewelryStoreUI.Pages.Offers.DetailsModel
@{
}

<h2>Offer Details</h2>

<div>
    <h4>Offer</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">OfferId</dt>
        <dd class="col-sm-10">@Model.Offer.OfferId</dd>
        <dt class="col-sm-2">OfferPercent</dt>
        <dd class="col-sm-10">@Model.Offer.OfferPercent</dd>
        <dt class="col-sm-2">Constraints</dt>
        <dd class="col-sm-10">@Model.Offer.Constraints</dd>
    </dl>
</div>
<div>
    <a asp-page="Edit" asp-route-id="@Model.Offer.OfferId">Edit</a> |
    <a asp-page="Index">Back to List</a>
</div>
